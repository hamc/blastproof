# Dependencies
node_modules/

# Build output
dist/

# Test/coverage output
coverage/

# blastproof runtime artifacts
.blastproof/reports/
.blastproof/sessions/

# Captured sessions are live credentials — never commit them.
.blastproof/.auth-state.json
.blastproof/auth.json

# Env / secrets
.env
.env.*

# Logs
*.log

# OS/editor
.DS_Store
.idea/
.vscode/

# Scratch space for the qa role (see .claude/agents/qa.md). Verification files
# written here are throwaway; an agent interrupted mid-run cannot clean up after
# itself, and `git add -A` would otherwise sweep its scratch into a commit.
e2e/
